package object;

//automatically public methods but I want to limit the visibility so I create abstract methods in the inherited objects instead

interface ObjectInterface {
	
	abstract void setDefaultAttributes();
	
}
